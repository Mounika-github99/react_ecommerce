// Not required when using modal form in UserList, but present for structure
import React from "react";

const UserForm = () => <div>Use UserList modal to add/edit users.</div>;

export default UserForm;
